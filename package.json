{
  "name": "starchess",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev && bun backend",
    "dev:network": "astro dev --host && bun backend",
    "backend": "cd backend/gameServer && bun start && cd ../..",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/svelte": "^1.0.2",
    "astro": "^1.5.2",
    "svelte": "^3.52.0"
  },
  "devDependencies": {
    "sass": "^1.56.1",
    "svelte-preprocess": "^4.10.7",
    "svelte-switch-case": "^2.0.0"
  }
}